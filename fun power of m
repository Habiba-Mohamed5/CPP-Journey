#include <iostream>
#include <cmath>
using namespace std;

// الفانكشن بتستقبل أطوال الأضلاع التلاتة
float powFun(float a,float b){
    
    return pow(a,b);
}

int main() 
{
   float a, b;
   
   cout << "Enter a:" << endl;
   cin >> a;
   cout << "Enter b:" << endl;
   cin >> b;
   
   
   
   // بصينا التلات متغيرات للفانكشن زي ما هي مستنية مننا بالظبط
   cout << "pow "<<powFun(a,b)<<endl;
   
   return 0;
}
